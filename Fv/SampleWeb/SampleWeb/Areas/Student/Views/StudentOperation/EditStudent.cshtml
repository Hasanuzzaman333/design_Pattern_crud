@model SampleData.Core.Student
@{
    ViewData["Title"] = "EditStudent";
    Layout = "~/Views/Shared/_CustomLayout.cshtml";
}

    <form role="form" action="/Student/StudentOperation/EditStudent" method="post">
        <input type="hidden" asp-for="@Model.Id" class=" form-control" />

        <div class="form-group">

            <label for="exampleInputEmail1">Student Name</label>
            @*@Html.DisplayFor(x => x.StudentName, new { @class = "form-control"})*@
            <input type="text" asp-for="@Model.StudentName" class=" form-control" />
            @*<input type="text" class="form-control" id="" name="StudentName" placeholder="Enter Name">*@
            @*<small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>*@
        </div>
        <div class="form-group">
            <label for="exampleInputEmail1">Student Age</label>
            <input type="text" asp-for="@Model.Age"  class=" form-control" />
            @*<small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>*@
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>

